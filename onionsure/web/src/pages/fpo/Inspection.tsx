import InspectionStudio from '../../components/InspectionStudio';

export default function FpoInspection() {
  return (
    <InspectionStudio
      role="fpo"
      title="Lot Inspection"
      subtitle="Verify a lot's quality before it reaches the procurement center — live OnionCheck detection."
    />
  );
}
